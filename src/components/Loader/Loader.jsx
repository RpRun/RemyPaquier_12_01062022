import './Loader.scss';

const Loader = () => {
  return (
    <div class="loader">
      <div class="spinner">
        <div class="spinner-bg"></div>
      </div>
    </div>
  );
};

export default Loader;
